#pragma once
#include <string>

// int playHangman(); // lance le jeu
bool isInWord(char character, std::string word); // vérifie si une chaine de caractère est dans une autre
// char* randWord(); // renvoie un mot aléatoire
